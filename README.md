# cst-events-site
A simple CI/CD pipeline project using GitHub Actions that builds and deploys a dynamic events website. The project uses Python to generate HTML from JSON data, runs automated tests with pytest, and publishes the site to GitHub Pages on every successful merge to the main branch.
